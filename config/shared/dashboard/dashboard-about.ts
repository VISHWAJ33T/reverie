import { DashBoardType } from "@/types";
import { FileText } from "lucide-react";

const dashBoardAbout: DashBoardType = {
  title: "About page",
  slug: "/settings/about",
  icon: FileText,
};

export default dashBoardAbout;

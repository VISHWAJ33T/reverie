import { DashBoardType } from "@/types";
import { Users } from "lucide-react";

const dashBoardUsers: DashBoardType = {
  title: "Users",
  slug: "/settings/users",
  icon: Users,
};

export default dashBoardUsers;
